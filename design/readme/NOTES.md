# README previews

Three proposed README directions, pending selection. The published README is unchanged.

Screenshots render the application's existing QML interface, treemap and theme adapter with an illustrative controller fixture. Folder names, paths, counts, capacity and sizes are invented demo data. No real filesystem scan or deletion was used for these images. The images are unretouched window captures at 1440 × 900.

- `dark-hero.png`: treemap and inspector, Tokyo Night.
- `dark-folders.png`: folder list beside the map, Tokyo Night.
- `dark-review.png`: cleanup review dialog, Tokyo Night.
- `light-hero.png`: the same data and layout, Catppuccin Latte.

Preview layouts use ordinary GitHub README elements: headings, paragraphs, links, images, tables, code blocks and disclosure sections. The surrounding comparison page is not proposed README styling.
